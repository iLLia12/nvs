DEMO - http://104.207.130.123/

To get the app run and running locally:

run client
- cd client
- npm i
- npm run dev

postgres and pgAdmin containers(or use your local setup):
docker-compose up -d

pgAdmin: localhost:5050
 - create server 
      1. host name/address: db,
      2. maintenance database: test_db
      3. username: root
      4. password: root

run server:
- cd server
- npm i
- node index.js

run migrations and seeders:
- cd server
- npm run db:refresh